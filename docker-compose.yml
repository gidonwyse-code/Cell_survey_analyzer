services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data:ro
    environment:
      DATA_DIR: /app/data
      CORS_ORIGINS: "*"

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
